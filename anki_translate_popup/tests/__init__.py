"""Test suite for the Translate & Pronounce Popup add-on.

Run from the repository root (the directory containing `anki_translate_popup`):

    python -m unittest discover -s anki_translate_popup/tests -t .

No network access is used: every provider test stubs the HTTP layer.
"""
